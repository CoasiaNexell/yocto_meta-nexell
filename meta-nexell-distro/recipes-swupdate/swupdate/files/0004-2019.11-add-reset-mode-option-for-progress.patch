diff --git a/tools/swupdate-progress.c b/tools/swupdate-progress.c
index 579f167..3f0d691 100644
--- a/tools/swupdate-progress.c
+++ b/tools/swupdate-progress.c
@@ -24,6 +24,9 @@
 #include <getopt.h>
 
 #include <progress_ipc.h>
+#include <linux/reboot.h>
+#include <sys/syscall.h>
+#include <sys/reboot.h>
 
 #define PSPLASH_MSG_SIZE	64
 
@@ -61,6 +64,7 @@ static struct option long_options[] = {
 	{"help", no_argument, NULL, 'h'},
 	{"psplash", no_argument, NULL, 'p'},
 	{"reboot", no_argument, NULL, 'r'},
+	{"mode", required_argument, NULL, 'm'},
 	{"wait", no_argument, NULL, 'w'},
 	{"color", no_argument, NULL, 'c'},
 	{"socket", required_argument, NULL, 's'},
@@ -75,6 +79,7 @@ static void usage(char *programname)
 	fprintf(stdout,
 		" -c, --color             : Use colors to show results\n"
 		" -r, --reboot            : reboot after a successful update\n"
+		" -m, --mode              : reboot mode (ex. recovery, bootloader, ...)\n"
 		" -w, --wait              : wait for a connection with SWUpdate\n"
 		" -p, --psplash           : send info to the psplash process\n"
 		" -s, --socket <path>     : path to progress IPC socket\n"
@@ -166,6 +171,22 @@ static void psplash_progress(char *pipe, struct progress_msg *pmsg)
 	free(buf);
 }
 
+static int system_reboot(const char *cmd)
+{
+	int ret;
+
+	fprintf(stdout, "%s: cmd = %s \n", __FUNCTION__, cmd );
+
+	if (!cmd)
+		ret = system("reboot");
+	else
+		ret = syscall(SYS_reboot,
+			LINUX_REBOOT_MAGIC1, LINUX_REBOOT_MAGIC2,
+			LINUX_REBOOT_CMD_RESTART2, cmd);
+	return ret;
+}
+
+
 int main(int argc, char **argv)
 {
 	int connfd;
@@ -183,9 +204,10 @@ int main(int argc, char **argv)
 	int opt_p = 0;
 	int c;
 	RECOVERY_STATUS	status = IDLE;		/* Update Status (Running, Failure) */
+	char *cmd = NULL;
 
 	/* Process options with getopt */
-	while ((c = getopt_long(argc, argv, "cwprhs:",
+	while ((c = getopt_long(argc, argv, "cwprhs:m:",
 				long_options, NULL)) != EOF) {
 		switch (c) {
 		case 'c':
@@ -200,6 +222,9 @@ int main(int argc, char **argv)
 		case 'r':
 			opt_r = 1;
 			break;
+		case 'm':
+			cmd = optarg;
+			break;
 		case 's':
 			SOCKET_PROGRESS_PATH = strdup(optarg);
 			break;
@@ -213,7 +238,7 @@ int main(int argc, char **argv)
 			break;
 		}
 	}
-		
+
 	tmpdir = getenv("TMPDIR");
 	if (!tmpdir)
 		tmpdir = "/tmp";
@@ -305,7 +330,7 @@ int main(int argc, char **argv)
 			psplash_ok = 0;
 			if ((msg.status == SUCCESS) && opt_r) {
 				sleep(5);
-				if (system("reboot") < 0) { /* It should never happen */
+				if (system_reboot(cmd) < 0) { /* It should never happen */
 					fprintf(stdout, "Please reset the board.\n");
 				}
 			}
